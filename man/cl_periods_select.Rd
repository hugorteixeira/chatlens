% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{cl_periods_select}
\alias{cl_periods_select}
\title{Select period rows by key, period, or range selectors}
\usage{
cl_periods_select(
  periods,
  select = NULL,
  period = NULL,
  text_col = "text_enriched",
  data_type = c("default", "simple", "aggregate")
)
}
\arguments{
\item{periods}{Data frame produced by [cl_chat_split_periods()] or a `chatlens_chat`}

\item{select}{Optional selectors. Accepts numeric row indexes, period names,
exact keys (for example `"2025-01"`), `period:key` selectors, or ranges
such as `"2025-01:2025-03"`.}

\item{period}{Alias for `select`. If `periods` is a chat object, this
controls which periods are generated before selection.}

\item{text_col}{Text column used when `periods` is a chat object}

\item{data_type}{Output type:
`"default"` returns selected period rows as a data frame,
`"aggregate"` returns selected rows without `period`/`key`,
`"simple"` returns one-row data frame with merged `text` and without
`period`/`key`.}
}
\description{
Select period rows by key, period, or range selectors
}
