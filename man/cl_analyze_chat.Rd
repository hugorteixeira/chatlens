% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{cl_analyze_chat}
\alias{cl_analyze_chat}
\title{Analyze prepared chat inputs}
\usage{
cl_analyze_chat(
  prepared,
  prompt,
  service = NULL,
  model = NULL,
  save = TRUE,
  return = c("standard", "genflow"),
  ...
)
}
\arguments{
\item{prepared}{A \code{chatlens_analysis_input} from \code{\link[=cl_prepare_analysis]{cl_prepare_analysis()}}, or
a \code{chatlens_chat} to prepare with default settings.}

\item{prompt}{Prompt or instruction for the model}

\item{service}{Optional model provider passed to \code{genflow}}

\item{model}{Optional model identifier}

\item{save}{Whether to save prompt, result, metadata, and run files}

\item{return}{Either \code{"standard"} or \code{"genflow"}}

\item{...}{Additional arguments passed to \code{\link[genflow:gen_txt]{genflow::gen_txt()}}}
}
\value{
With \code{return = "standard"}, a \code{chatlens_analysis_result} data frame.
With \code{return = "genflow"}, the raw \code{genflow} response for one input or a
list of raw responses for multiple inputs.
}
\description{
\code{cl_analyze_chat()} sends each prepared input to \code{genflow::gen_txt()} using
\code{prompt} plus the prepared chat text. If \code{prepared} is a \code{chatlens_chat}, it
is first passed through \code{cl_prepare_analysis()} with default settings.
}
\details{
With \code{save = TRUE}, each prepared input directory receives timestamped
\verb{prompt_*}, \verb{result_*}, \verb{meta_*}, and \verb{run_*} files. The run id includes the
creation time, service, and model.
}
