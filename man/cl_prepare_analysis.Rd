% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{cl_prepare_analysis}
\alias{cl_prepare_analysis}
\title{Prepare compact chat inputs for LLM analysis}
\usage{
cl_prepare_analysis(
  chat,
  period = c("all", "year", "month", "week", "day"),
  select = NULL,
  formatting = c("simple", "raw"),
  save = TRUE,
  cache_dir = NULL,
  text_col = "text_enriched"
)
}
\arguments{
\item{chat}{A \code{chatlens_chat} object}

\item{period}{One of \code{"all"}, \code{"year"}, \code{"month"}, \code{"week"}, or \code{"day"}}

\item{select}{Optional period selector. Character selectors can be exact keys
such as \code{"2020-10"} or \code{"2020-10-15"}, ranges such as
\code{"2020-01:2020-03"}, or broader prefixes where supported. For example,
with \code{period = "day"}, \code{"2020-10"} selects each day in October 2020.}

\item{formatting}{Text formatting. \code{"simple"} is the default; \code{"raw"} keeps
one line per message.}

\item{save}{Whether to save prepared inputs}

\item{cache_dir}{Optional cache directory. When \code{NULL}, uses the chat's
stored cache location or \verb{~/.chatlens}.}

\item{text_col}{Text column used as transcript content}
}
\value{
A \code{chatlens_analysis_input} data frame with one row per prepared
input and columns for the period key, prepared text, and saved input paths.
}
\description{
\code{cl_prepare_analysis()} is the last step before \code{cl_analyze_chat()}. It can
keep the whole chat as one input or split it into year, month, week, or day
inputs. With \code{save = TRUE}, each input is written as \code{input.txt} and
\code{input.rds} under the chat analysis cache.
}
\details{
Default \code{"simple"} formatting groups repeated dates and consecutive messages
from the same sender. \code{"raw"} preserves one formatted chat line per message.
}
